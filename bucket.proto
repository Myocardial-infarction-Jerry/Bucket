syntax = "proto3";

service tokenAuth {
    rpc tokenPush(token) returns (authResult) {}
}

message token {
    string token = 1;
}

message authResult {
    bool authState = 1;
    string message = 2;
}